import { exportAllDeclaration } from '@babel/types';
import test from 'node:test';
import ExchangeRate from './../src/exchange.js';

describe('ExchangeRate', () => {

  test('should correctly return the currency amount input by the user in exchanged rate', () =>
  {
    const response = new ExchangeRate(begin);
    expect(currency).toEqual()

  }
  )
})